<% const body = `
  ${error ? `<div class="demo-error">${error}</div>` : ``}

  <form method="post" action="/login" class="demo-form">
    <label>E-mail</label>
    <input name="email" type="email" value="${email || ""}" required />

    <label>Wachtwoord</label>
    <input name="password" type="password" required />

    <button class="demo-btn" type="submit">INLOGGEN</button>

    <a class="demo-link" href="/signup">Account aanmaken</a>
  </form>
` %>

<%- include("_auth_shell", { title: "Login - MyPunctoo", stepTitle: "STAP 1: ACCOUNT.", body }) %>
