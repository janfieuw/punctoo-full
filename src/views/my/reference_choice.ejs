<% const body = `
  <div class="card">
    <div class="h1">Referentietijden</div>
    <div class="p">Werknemer: <b>${employee.employee_name}</b>. Kies hoe je referentietijden wil instellen voor correcte rapporten.</div>

    <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px">
      <div class="card" style="margin:0">
        <div class="h2">ROOSTER (weekpatroon)</div>
        <div class="p">Vaste weekindeling (ma–zo). Ideaal voor stabiele werkuren.</div>
        <div class="p">Status: <b>${patternCount > 0 ? "ingesteld" : "nog leeg"}</b></div>
        <a class="btn" href="/app/employees/${employee.id}/reference/rooster">Open rooster</a>
      </div>

      <div class="card" style="margin:0">
        <div class="h2">KALENDER (per datum)</div>
        <div class="p">Specifieke tijden per dag (handig voor uitzonderingen).</div>
        <div class="p">Status: <b>${calendarCount > 0 ? "ingesteld" : "nog leeg"}</b></div>
        <a class="btn" href="/app/employees/${employee.id}/reference/kalender">Open kalender</a>
      </div>
    </div>

    <div style="margin-top:14px">
      <a class="btn ghost" href="/app/employees">Terug naar werknemers</a>
    </div>
  </div>
` %>

<%- include("../_layout", {
  title: "Referentietijden - MyPunctoo",
  badge: company.company_name,
  topLink: { href: "/app/employees", label: "Terug" },
  logoutAction: "/logout",
  body
}) %>
