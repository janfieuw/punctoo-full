<% const body = `
  <div class="card">
    <div class="h1">Admin dashboard</div>
    <div class="p">Overzicht van nieuwe acties.</div>
    <div class="kpi">
      <div class="pill-light">Nieuwe inschrijvingen: ${kpis.newSignups}</div>
      <div class="pill-light">Nieuwe bestellingen: ${kpis.newOrders}</div>
    </div>
    <div class="hr"></div>
    <a class="btn accent" href="/admin/customers">Klanten openen</a>
  </div>
` %>

<%- include("_layout", { title:"Admin", badge:"Admin", logoutAction:"/admin/logout", body }) %>
